# KOS-vtol

run vtol(X, Y).
  [optional] X = max cycles.
  [optional] Y = boost IPU.
  
all engines ment for vtol must have there tag set to "vtol" (no quotes).

known bugs:
  it dose not work if the CoM is below the CoT
